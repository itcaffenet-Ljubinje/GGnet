name: ggnet
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - fastapi
      - uvicorn[standard]
      - sqlalchemy
      - asyncpg
      - alembic
      - python-dotenv
      - passlib[bcrypt]
      - jinja2
