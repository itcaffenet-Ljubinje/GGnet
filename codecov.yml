coverage:
  status:
    project:
      default:
        # Require coverage to not decrease
        target: 80%
        threshold: 1%
        base: auto
        branches:
          - main
          - develop
        flags:
          - backend
          - frontend
    patch:
      default:
        # Allow patch coverage to decrease slightly
        target: 70%
        threshold: 2%
        base: auto
        branches:
          - main
          - develop

comment:
  layout: "header,diff,files,footer"
  behavior: default
  require_changes: false
  require_base: no
  require_head: yes

github_checks:
  annotations: true

ignore:
  - "backend/venv/"
  - "frontend/node_modules/"
  - "backend/tests/"
  - "frontend/src/test/"
  - "*.pyc"
  - "*.pyo"
  - "*.pyd"
  - "__pycache__"
  - ".git"
  - ".coverage"
  - "coverage.xml"
  - "htmlcov/"
  - ".pytest_cache/"
  - ".mypy_cache/"
  - ".tox/"
  - "dist/"
  - "build/"
  - "*.egg-info/"

flags:
  backend:
    paths:
      - backend/app/
    carryforward: true
  frontend:
    paths:
      - frontend/src/
    carryforward: true
